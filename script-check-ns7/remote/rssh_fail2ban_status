#!/usr/bin/env bash
# Remote launcher per check_fail2ban_status.sh
# Scarica e esegue la versione completa da repository

REPO_URL="https://raw.githubusercontent.com/Coverup20/checkmk-tools/main/script-check-ns7/full/check_fail2ban_status.sh"

# Exec sostituisce il processo corrente, output immediato senza buffering
exec bash <(curl -fsSL "$REPO_URL") "$@"
