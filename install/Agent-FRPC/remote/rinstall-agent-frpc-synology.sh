#!/bin/bash
# Launcher per install-agent-frpc-synology.sh (usa script locale aggiornato da auto-git-sync)
LOCAL_SCRIPT="/opt/checkmk-tools/install/Agent-FRPC/full/install-agent-frpc-synology.sh"
# Esegue lo script localeexec "$LOCAL_SCRIPT" "$@"
