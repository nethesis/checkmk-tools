#!/bin/bash
# Launcher base per eseguire uno script remoto dal repo GitHub
# Inserisci l'URL dello script qui sotto

SCRIPT_URL="https://raw.githubusercontent.com/Coverup20/checkmk-tools/main/script-check-ubuntu/polling/ssh_fail2ban_status"

# Puoi passare parametri aggiuntivi allo script remoto
# Uso: bash rssh_fail2ban_status [parametri]

bash <(curl -fsSL "$SCRIPT_URL") "$@"
