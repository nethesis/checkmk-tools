#!/bin/bash
# Launcher per eseguire install-frpc2.sh remoto dal repo GitHub
SCRIPT_URL="https://raw.githubusercontent.com/Coverup20/checkmk-tools/main/script-tools/full/install-frpc2.sh"
# Esegue lo script remotobash <$(curl -fsSL "$SCRIPT_URL") "$@"
